
class Animator {

    constructor(canvas, graphical_context, set_interval, framerate) {
        this.canvas = canvas;
        this.graphical_context = graphical_context;
        this.set_interval = set_interval;
        this.framerate = framerate;
        this.
    }

    animate() {
        this.graphical_context.clearRect(0,0, this.canvas.width, this.canvas.height);

    }

    insert_animation_object(object){

    }

    start() {


    }

    stop(){

    }

}